
def identifier_meilleur_etudiant(self):
    self.cursor.execute("""
        SELECT student_id, AVG(note) as moyenne 
        FROM grades 
        GROUP BY student_id 
        ORDER BY moyenne DESC 
        LIMIT 1
    """)
    return self.cursor.fetchone() # Renverra un tuple comme (14, 18.5)